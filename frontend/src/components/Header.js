import React from "react";
import PropTypes from "prop-types";
import Nav from "./Nav";

const Header = (props) => {
  return (
    <>
      <h1>MERN Todo App</h1>
      <Nav />
    </>
  );
};

export default Header;
